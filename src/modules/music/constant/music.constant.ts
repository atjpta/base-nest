import { AppConstant } from 'src/modules/app/constant/app.constant';

export class MusicConstant {
  static SWAGGER_TAG = `music`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/musics`;
  static MODEL_NAME = `musics`;
}
