import { AppConstant } from 'src/modules/app/constant/app.constant';

export class PlaylistConstant {
  static SWAGGER_TAG = `playlist`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/playlists`;
  static MODEL_NAME = `playlists`;
}
