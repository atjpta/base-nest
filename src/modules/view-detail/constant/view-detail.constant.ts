import { AppConstant } from 'src/modules/app/constant/app.constant';

export class ViewDetailConstant {
  static SWAGGER_TAG = `view-details`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/view-details`;
  static MODEL_NAME = `view_details`;
}
