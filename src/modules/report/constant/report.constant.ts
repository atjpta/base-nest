import { AppConstant } from 'src/modules/app/constant/app.constant';

export class ReportConstant {
  static SWAGGER_TAG = `reports`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/reports`;
  static MODEL_NAME = `reports`;
}
