import { AppConstant } from 'src/modules/app/constant/app.constant';

export class SingerConstant {
  static SWAGGER_TAG = `singer`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/singers`;
  static MODEL_NAME = `singers`;
}
