import { AppConstant } from 'src/modules/app/constant/app.constant';

export class FileConstant {
  static SWAGGER_TAG = `file`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/file`;
  static FILE_FIELD_NAME = 'file';
  static FOLDER = 'uploads';
}
