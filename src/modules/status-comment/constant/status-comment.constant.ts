import { AppConstant } from 'src/modules/app/constant/app.constant';

export class StatusCommentConstant {
  static SWAGGER_TAG = `status-comments`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/status-comments`;
  static MODEL_NAME = `status_comments`;
}
