import { AppConstant } from 'src/modules/app/constant/app.constant';

export class NewsConstant {
  static SWAGGER_TAG = `news`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/news`;
  static MODEL_NAME = `news`;
}
