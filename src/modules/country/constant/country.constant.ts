import { AppConstant } from 'src/modules/app/constant/app.constant';

export class CountryConstant {
  static SWAGGER_TAG = `country`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/countries`;
  static MODEL_NAME = `countries`;
}
