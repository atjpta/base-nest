import { AppConstant } from 'src/modules/app/constant/app.constant';

export class ArtistConstant {
  static SWAGGER_TAG = `artist`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/artists`;
  static MODEL_NAME = `artists`;
}
