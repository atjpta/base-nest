import { AppConstant } from 'src/modules/app/constant/app.constant';

export class SongConstant {
  static SWAGGER_TAG = `songs`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/songs`;
  static BUCKETS = 'songs';
}
