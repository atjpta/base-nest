import { AppConstant } from 'src/modules/app/constant/app.constant';

export class GenreConstant {
  static SWAGGER_TAG = `genre`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/genres`;
  static MODEL_NAME = `genres`;
}
