import { AppConstant } from 'src/modules/app/constant/app.constant';

export class UserConstant {
  static SWAGGER_TAG = `user`;
  static API_PREFIX = AppConstant.APP_PREFIX + `/users`;
  static MODEL_NAME = `users`;
}
